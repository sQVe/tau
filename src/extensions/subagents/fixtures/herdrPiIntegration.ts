// Test fixture standing in for the extension that `herdr integration install pi` writes.
// HERDR_INTEGRATION_ID=pi

import type { ExtensionAPI } from '@earendil-works/pi-coding-agent';

export default function herdrPiIntegration(_pi: ExtensionAPI) {
  // The marker above is the only behavior this fixture needs.
}
