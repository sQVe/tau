---
'tau': patch
---

Name subagent panes after their built-in profile, so reviewers show as `reviewer-*` instead of
`scout-*`. Custom profiles keep the role prefix. Saved `scout-*` and `investigator-*` workers stay
readable.
