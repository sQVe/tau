---
'tau': patch
---

Report every missing or non-file path in one `bulk_read` refusal instead of stopping at the first.
Add coding instructions to run commands CC Safety Net may block in their own bash call and to append
`|| true` only to probes.
