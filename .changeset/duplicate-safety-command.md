---
'tau': patch
---

Fix Pi worker startup when multiple CC Safety Net installations register the same command. Keep
every configured integration and require the saved safety extension's exact source path.
