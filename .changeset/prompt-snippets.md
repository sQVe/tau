---
'tau': minor
---

Add prompt snippets. Press `alt+s` or run `/snippets` to pick single-purpose instructions that are
added before or after your next message. Toggles turn off after each send. Snippet files are read
from disk on every send, so edits apply without reloading Pi.
