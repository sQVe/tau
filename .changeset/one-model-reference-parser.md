---
'tau': patch
---

Show the full model ID after the provider in worker pane titles, such as `meta-llama` instead of
`llama` for `openrouter/meta/llama`. Task IDs and model references otherwise accept exactly what
they did before.
