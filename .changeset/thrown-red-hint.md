---
'tau': minor
---

Hint when a focused test reaches RED through a thrown `TypeError`, `ReferenceError`, `SyntaxError`,
or missing module instead of a failed assertion. The run still counts as RED.
