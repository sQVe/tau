---
'tau': patch
---

Review large commits in several bounded comment review calls instead of rejecting them, and drop the
300-file limit. Commits that change submodules or delete files no longer fail review.
