---
'tau': patch
---

Tell agents to drop backwards compatibility by default: update the callers and delete the old path.
Ask first when a published package, API, or CLI, a user-owned config file, or saved data may depend
on the old surface.
