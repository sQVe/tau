---
'tau': minor
---

Add the handoff skill. When work belongs in another workspace, the agent sends a one-way message
through a file to the herdr agent there instead of editing, testing, or committing that worktree
itself.
