---
'tau': minor
---

Add the handoff skill. When work belongs in another worktree, the agent finds or starts the herdr
agent that owns it, sends a brief through a file, and waits for one reply instead of editing,
testing, or committing that worktree itself.
