---
'tau': patch
---

Start Pi in the target workspace's manager pane when a handoff finds no agent there, instead of
asking the user. The handoff skill still asks when several agents match, or when the workspace's
first tab has several panes or none.
