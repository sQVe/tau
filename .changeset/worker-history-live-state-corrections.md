---
'tau': patch
---

Keep the worker history deadline countdown for live states only. Stopped, unconfirmed, untracked,
and unknown workers show the deadline as a clock fact instead of implying work is still counting
down. List and detail navigation accept `home`, `end`, and encoded `shift+g` through the shared vim
predicates.
