---
'tau': patch
---

Stop refusing tools in a bare repository root. The system prompt rule remains and now allows writing
handoff messages under `.tau/handoffs`, so a manager in the root can hand off work.
