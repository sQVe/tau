---
'tau': minor
---

Search the snippet menu. Press `/` and type to filter snippets by name and description, then press
Enter or `esc` to return to the list with the filter kept. In the list, `esc` clears the filter
before it closes the menu. Snippets turned on stay on while the filter hides them.
