---
'tau': minor
---

Commit without human approval in every mode. Remove the approval overlay, message editor, and
startup preapproval flag. Failed message checks and blocking comment reviews return tool errors.
Preparation-added files require explicit assignment in a new call. Preparation, recovery, and checks
remain enabled.
