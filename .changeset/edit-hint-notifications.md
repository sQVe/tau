---
'tau': patch
---

Show new edit/write TDD hints as nonblocking info notifications in Pi UI sessions. Keep the hint in
agent-visible result text. Existing deduplication still prevents repeated notices, and headless
sessions do not notify.
