---
'tau': minor
---

Add a `worktree` skill that creates or opens a Grove worktree in Herdr for a ticket or task, and an
`update-branch` skill that rebases onto the base branch, resolves conflicts, and pushes with a lease
only when asked to update the PR. `/bro <topic>` now explains that topic in plain language instead
of restating the last response.
