---
'tau': minor
---

Run root `scripts.fix`, when available, before commit staging and candidate checks. Report missing
or failing fixers. Make Tau's staged hook check lint and formatting without rewriting approved
files.
