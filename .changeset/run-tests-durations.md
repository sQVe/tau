---
'tau': minor
---

Show test durations in `run_tests`. A focused run shows the duration of each selected test. A full
run lists only tests that take over 1000 ms, slowest first, and skips `.integration.` files.
