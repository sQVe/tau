---
'tau': patch
---

Stop rescanning the reservation tree for each widget history row. Widget refreshes read current task
records without a cache; the status tool keeps its descendant reservation checks.
