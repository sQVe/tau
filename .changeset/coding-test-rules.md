---
'tau': minor
---

Add three test rules to the coding instructions. Each test defends one behavior a caller can
observe. A refusal test asserts the error and that nothing else changed. Assertions stay out of
fakes and callbacks whose errors production code may catch.
